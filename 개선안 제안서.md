# 개선안 제안서

## 결론

기본안의 “마커마다 prefab을 켜고 끄는 방식”은 유지한다. 다만 경로 prefab을 각 인식 이미지에 개별적으로만 붙이는 방식 대신, **건물 전체의 설계 좌표계 하나와 AR 좌표계 하나를 마커로 정렬하는 방식**을 적용하는 것을 권장한다. 이 방식은 여러 마커 사이의 선로 연결을 자연스럽게 보이고, 다른 마커를 다시 인식할 때 AR 드리프트를 보정할 수 있다.

## 기본안의 한계

- Unity의 `(1, 1, 1)`이 실제 1m와 같은 스케일이어도, AR 세션이 시작된 원점과 건물 설계 좌표의 원점은 자동으로 일치하지 않는다.
- 각 마커에 콘텐츠를 독립적으로 붙이면, 마커 A의 선로와 마커 B의 선로가 경계에서 미세하게 어긋날 수 있다.
- 인식 때마다 콘텐츠 전체를 해당 마커 Pose로 직접 옮기면, 이전 마커에서 정렬된 콘텐츠가 크게 점프할 수 있다.

## 제안 구조

### 1. 건물 로컬 좌표계

`BuildingContentRoot` 아래에 모든 경로 prefab과 가상 마커 Transform을 둔다. 이 좌표계는 실제 건물의 설계 좌표를 미터 단위로 표현한다.

- `Marker_01`~`Marker_10`은 건물에서의 알려진 위치·회전이다.
- 각 경로 prefab은 개별 마커가 아니라 `BuildingContentRoot`의 자식으로 배치하며, 설계 좌표 기준 위치를 가진다.
- 최대 3개 표시 규칙은 각 prefab의 활성화 상태에만 적용한다.

### 2. 마커로 AR 좌표계 보정

마커 `n`이 추적되면 다음 두 Pose를 비교한다.

- `knownMarkerPose`: `BuildingContentRoot` 기준으로 미리 배치한 `Marker_n`의 Pose
- `trackedMarkerPose`: 카메라가 현재 인식한 실제 마커의 AR 월드 Pose

두 Pose가 일치하도록 `BuildingContentRoot`의 위치·회전을 보정한다. 개념적으로는 다음 변환이다.

`보정된 콘텐츠 좌표 = trackedMarkerPose × inverse(knownMarkerPose) × 콘텐츠 로컬 좌표`

따라서 한 마커를 기준으로 건물 전체 콘텐츠가 AR 공간의 올바른 위치에 놓이고, 이후 활성화되는 경로 prefab도 같은 좌표계 안에서 연결된다.

### 3. 드리프트 보정 원칙

- `Tracking` 상태이고 추적 품질이 충분한 마커에서만 보정한다.
- 처음 인식한 마커에서는 즉시 정렬해 초기 기준을 잡는다.
- 이후 마커에서는 한 프레임에 큰 이동이 발생하지 않게 짧은 시간 동안 위치·회전을 보간한다. 단, 오차가 큰 경우에는 즉시 보정하거나 “재정렬 중” 안내를 표시한다.
- 이미지가 잠시 `Limited` 또는 `None` 상태가 되어도 마지막 안정 Pose를 유지한다. 콘텐츠를 즉시 끄거나 원점으로 되돌리지 않는다.
- 여러 마커가 동시에 보이면 신뢰도가 가장 높은 하나만 기준으로 사용한다. 한 프레임에 여러 보정을 적용하지 않는다.

## 데이터 모델 제안

각 마커를 배열 인덱스에 의존하지 않고 하나의 데이터 항목으로 관리한다.

| 필드 | 설명 |
| --- | --- |
| `markerName` | Reference Image Library의 이미지 이름 |
| `routeOrder` | 선로 진행 순서. 0부터 시작 |
| `knownMarkerTransform` | 건물 로컬 좌표에서의 마커 Pose |
| `routePrefab` | 이 구간에서 표시할 prefab |
| `runtimeInstance` | 시작 시 생성해 둔 인스턴스 |

이 구조라면 마커 이름, 실제 경로 순서, 배열 순서가 달라도 잘못된 prefab이 연결되는 문제를 피할 수 있다.

## 표시 정책

현행 요구대로 현재 `routeOrder`와 이전 두 구간만 활성화한다. 단, 경로가 갈라지는 건물에서는 단순한 번호 순서 대신 다음 데이터를 추가할 수 있다.

- `previousRouteIds`: 현재 구간으로 들어오는 이전 구간 목록
- `nextRouteIds`: 다음 구간 목록
- `routeGroup`: 층·방향·설비별 분류

초기 버전은 선형 경로(현재 + 이전 2개)로 시작하고, 분기 경로 요구가 생길 때 그래프 데이터로 확장한다.

## 구현 우선순위

1. Reference Image Library와 마커 데이터 10개를 설정한다.
2. 모든 prefab을 한 번 생성하고 3개 표시 정책을 구현한다.
3. 첫 마커 인식으로 `BuildingContentRoot`를 정렬한다.
4. 다른 마커 재인식 시 보간 기반 재정렬을 추가한다.
5. 실제 현장에서 마커 간 선로가 자연스럽게 이어지는지 측정하고 가상 마커 좌표를 보정한다.

## 사용자 설정 및 현장 검증

- 가상 마커 Transform은 줄자·도면 등 신뢰 가능한 기준으로 측정해 배치한다. Unity 좌표 1단위는 현실 1m로 둔다.
- 모든 마커의 실제 크기는 Image Library 설정값과 일치해야 한다.
- 인쇄 마커는 평평하게 부착하고, 유사한 반복 무늬·강한 반사광·어두운 조명을 피한다.
- 실제 기기에서 마커 1→2→3→10 순으로 이동한 뒤, 다시 1번을 인식해 콘텐츠가 재정렬되는지 확인한다.
- 보정 시 눈에 띄는 점프가 있으면 해당 마커의 가상 Pose, 실제 인쇄 크기, prefab의 원점을 먼저 점검한다.
