# 기획 기본구조

## 1. 목적

건물 내부의 인터넷 선로를 AR로 확인하는 Android Unity 앱이다. 사용자가 벽·바닥 등에 설치한 마커 이미지를 카메라로 인식하면, 해당 위치를 기준으로 미리 만든 선로 안내 prefab을 현실 위에 표시한다. 복잡한 전체 선로 대신 현재 위치와 직전 경로를 최대 3개만 보여 주어 화면을 읽기 쉽게 유지한다.

## 2. 사용자 흐름

1. 사용자는 `LoginScene`에서 `ARScene`으로 이동한다.
2. 카메라 권한을 허용하고 AR 세션을 시작한다.
3. 카메라가 마커 1~10 중 하나를 인식한다.
4. 앱은 해당 번호의 prefab을 활성화하고, 마커에 맞춰 위치·회전을 맞춘다.
5. 현재 번호와 앞선 두 번호의 prefab만 표시한다.
6. 사용자가 다른 마커로 이동하면 표시 대상과 위치 보정을 갱신한다.

`ExplaneScene`에는 위 사용법, 마커를 카메라에 충분히 비추는 방법, 인식이 되지 않을 때의 확인 사항을 텍스트로 안내한다.

## 3. 사전 준비 데이터

마커 번호는 0~9의 내부 인덱스와 1~10의 사용자 표기를 함께 사용한다. Inspector 배열의 같은 인덱스는 반드시 같은 마커를 의미한다.

| 데이터 | 개수 | 인덱스 규칙 | 용도 |
| --- | ---: | --- | --- |
| Reference Image Library 이미지 | 10 | 이미지 1 = 인덱스 0 | AR 이미지 추적 |
| 경로 prefab 라이브러리 | 10 | prefab 1 = 인덱스 0 | 선로·안내 오브젝트 |
| 가상 마커 위치 목록 | 10 | 위치 1 = 인덱스 0 | 각 마커의 기준 위치/회전 |
| 실행 중 생성 오브젝트 목록 | 10 | 생성본 1 = 인덱스 0 | 활성화 상태 관리 |

각 이미지의 추적 이름은 `Marker_01`부터 `Marker_10`까지로 통일한다. 코드에서는 이름에서 번호를 찾기보다, Inspector에서 마커 이름과 prefab·가상 위치를 하나의 항목으로 묶어 관리하는 것을 권장한다.

## 4. Scene 구성

`ARScene`에는 다음을 준비한다.

- `AR Session`, `AR Session Origin`(또는 사용하는 AR Foundation 버전에 맞는 XR Origin)
- `AR Camera`
- `AR Tracked Image Manager`: Reference Image Library를 할당하고 최대 이동 이미지 수를 10으로 설정
- `RouteContentRoot`: 실행 중 생성한 경로 prefab의 부모
- `MarkerPoseRoot`: 가상 마커 위치 목록을 관리하는 빈 오브젝트. 자식으로 `Marker_01`~`Marker_10`의 빈 Transform을 두고, 각 위치·회전을 실제 건물의 설계 좌표와 동일하게 배치
- `MarkerRouteController`: 마커 이벤트, prefab 생성, 활성화 수를 관리하는 스크립트

경로 prefab은 `Assets/Data/Prefabs`처럼 Unity 프로젝트 안의 적절한 폴더에 저장한다. 새 C# 스크립트는 반드시 `Assets/Data/Scripts`에 만들며 `sh_` 접두사를 사용한다.

## 5. 실행 시 처리

1. `MarkerRouteController`가 경로 prefab 10개를 `RouteContentRoot` 아래에 한 번만 생성한다.
2. 생성한 오브젝트는 각각 실행 중 오브젝트 목록의 동일 인덱스에 저장하고 즉시 `SetActive(false)` 처리한다.
3. AR 이미지 추적 변경 이벤트를 구독한다.
4. 각 이미지 이벤트에서 마커 이름으로 인덱스를 확인하고, 추적 상태가 `Tracking`인 경우에만 표시 갱신을 요청한다.
5. Scene이 비활성화되거나 종료될 때 이벤트 구독을 해제한다.

prefab을 매 인식 때마다 새로 만들지 않는다. 미리 생성한 인스턴스를 활성화·비활성화하므로 중복 생성, 메모리 증가, 같은 선로의 중첩 표시를 막는다.

## 6. 마커 인식 및 위치 동기화

마커 `n`을 인식하면 다음을 수행한다.

1. 추적된 실제 이미지의 Pose(위치·회전)를 읽는다.
2. 가상 마커 위치 목록에서 `n`번 Transform을 읽는다.
3. `n`번 경로 prefab을 해당 마커에 연결된 기준 Transform 아래에 배치하거나, prefab 내부의 기준점을 마커 Pose에 맞춘다.
4. 경로 prefab은 제작 시 마커를 원점 `(0, 0, 0)`으로 삼아야 한다. 그러면 해당 prefab의 자식 선로들은 실제 마커 기준의 올바른 상대 위치에 표시된다.

마커를 다시 인식했을 때는 이미 활성화된 해당 prefab의 위치·회전을 최신 Pose로 갱신한다. 추적이 잠시 끊겼다고 곧바로 끄지 말고 마지막으로 안정적으로 인식한 Pose를 유지한다. 다른 마커가 인식되면 그 마커의 위치를 기준으로 표시를 갱신한다.

## 7. 최대 3개 표시 규칙

활성화 기준은 **현재 인식 마커와 이전 두 인덱스**다. 예를 들어 3번 마커(내부 인덱스 2)를 인식하면 3번·2번·1번 prefab을 표시한다.

| 현재 마커 | 활성 prefab |
| --- | --- |
| 1번 | 1번 |
| 2번 | 2번, 1번 |
| 3번 | 3번, 2번, 1번 |
| 7번 | 7번, 6번, 5번 |
| 10번 | 10번, 9번, 8번 |

처리 규칙은 다음과 같다.

1. 현재 인덱스를 `currentIndex`로 둔다.
2. 표시 범위는 `max(0, currentIndex - 2)`부터 `currentIndex`까지다.
3. 실행 중 생성 오브젝트 10개를 순회해 범위 안의 것만 활성화하고 나머지는 비활성화한다.
4. 사용자가 3번에서 2번으로 되돌아가면 3번은 꺼지고 2번·1번만 남는다.

이는 “최근 인식 순서”를 저장하는 일반 큐와 다르다. 요구된 예시처럼 현재 마커의 앞선 경로를 보이려면 인덱스 기반 3개 창이 정확하다. 실제 경로 순서가 마커 번호와 다르면, 번호 대신 `routeOrder` 값을 각 마커 데이터에 추가해 동일 규칙을 적용한다.

## 8. Inspector 설정 체크리스트

1. Reference Image Library에 마커 이미지 10장을 추가하고 이름을 `Marker_01`~`Marker_10`으로 설정한다.
2. 실제 출력 크기를 각 이미지에 입력한다. 크기가 틀리면 AR 콘텐츠 스케일과 위치가 부정확해진다.
3. `AR Tracked Image Manager`에 라이브러리를 연결한다.
4. `MarkerRouteController`의 마커 데이터 10개에 마커 이름, 경로 prefab, 가상 마커 Transform을 같은 순서로 할당한다.
5. 각 prefab은 마커 기준 상대 좌표로 제작한다. prefab의 원점과 마커 기준점이 일치하는지 확인한다.
6. 실제 건물에 마커를 부착할 때는 빛 반사, 주름, 가림이 적은 위치를 고르고, 가상 마커 Transform의 거리 단위는 실제 미터 단위와 맞춘다.

## 9. 검증 절차

1. Unity Console에 누락된 prefab·Transform·마커 이름 오류가 없는지 확인한다.
2. Editor에서 prefab 연결과 활성화 규칙을 확인한다. 카메라 이미지 추적은 실제 Android 기기에서 검증한다.
3. Android 기기에서 1번, 2번, 3번 마커를 차례로 인식해 표시 개수가 1개, 2개, 3개인지 확인한다.
4. 7번 마커를 인식해 7·6·5번만 보이는지 확인한다.
5. 3번에서 2번으로 돌아가 3번이 비활성화되고 2·1번만 남는지 확인한다.
6. 같은 마커를 반복 인식해도 prefab 인스턴스 수가 증가하지 않는지 확인한다.
7. 마커를 다시 비췄을 때 경로가 해당 마커 기준으로 재정렬되는지 확인한다.
