# 기획 기본구조

## 1. 목표와 최종 구현 방식

이 앱은 Android 기기에서 건물 내부의 인터넷 선로 위치를 AR로 안내합니다. 사용자가 설치된 마커 이미지를 비추면, 해당 구간과 이전 두 구간의 선로 prefab만 현실 위에 표시합니다.

최종 방식은 **건물 공통 좌표계 보정 방식**입니다. 경로 prefab을 마커별로 각각 추적 이미지에 붙이지 않습니다. 대신 모든 선로와 가상 마커 위치를 하나의 `BuildingContentRoot` 아래에 두고, 실제 마커가 인식될 때마다 이 루트를 AR 공간에 정렬합니다. 이렇게 하면 여러 prefab이 같은 건물 좌표계를 공유하므로 구간 사이의 선로가 자연스럽게 이어지고, 다른 마커를 다시 인식할 때 AR 위치 오차도 보정할 수 있습니다.

## 2. 사용자 흐름

1. 사용자는 `LoginScene`에서 `ARScene`으로 이동합니다.
2. 앱이 카메라 권한을 받고 AR 세션을 시작합니다.
3. 사용자가 마커 1~6 중 하나를 카메라에 비춥니다.
4. 앱은 인식된 실제 마커와 같은 번호의 가상 마커를 기준으로 `BuildingContentRoot`를 정렬합니다.
5. 현재 경로와 이전 두 경로의 prefab만 활성화합니다.
6. 사용자가 다음 마커로 이동하면 위치 보정과 표시 대상이 갱신됩니다.

`ExplaneScene`에는 마커 비추는 방법, 인식 실패 시 확인 방법, 재정렬 중 잠시 움직이지 말아야 한다는 안내를 텍스트로 표시합니다.

## 3. 핵심 용어

| 용어 | 의미 |
| --- | --- |
| 실제 마커 | 건물에 인쇄·부착한 이미지. AR 카메라가 인식하는 대상입니다. |
| 가상 마커 | Unity Scene에 빈 Transform으로 배치한 마커의 설계상 위치·회전입니다. |
| 건물 좌표계 | 실제 건물 도면을 미터 단위로 옮긴 Unity 로컬 좌표계입니다. |
| `BuildingContentRoot` | 건물 좌표계 전체를 담고 AR 공간에 정렬하는 부모 GameObject입니다. |
| 경로 prefab | 인터넷 선로와 안내 요소를 표현하는 미리 제작된 오브젝트입니다. |

Unity 좌표 1단위는 실제 거리 1m로 사용합니다. 다만 스케일이 같아도 AR 세션의 원점은 건물 원점과 다르므로, 마커 인식으로 두 좌표계를 정렬해야 합니다.

## 4. 사전 준비 데이터

마커 데이터는 단순 배열 인덱스만으로 연결하지 않고, 하나의 마커 데이터 항목으로 묶어 관리합니다. Inspector에 6개를 등록합니다.

| 필드 | 예시 | 용도 |
| --- | --- | --- |
| `markerName` | `Marker_01` | Reference Image Library 이미지 이름과 연결합니다. |
| `routeOrder` | `0`~`5` | 실제 선로 진행 순서입니다. 이미지 번호와 달라도 됩니다. |
| `knownMarkerTransform` | `Marker_01` Transform | 건물 좌표계 안의 가상 마커 위치·회전입니다. |
| `routePrefab` | `Route_01` | 이 구간에 표시할 선로 prefab입니다. |
| `runtimeInstance` | 실행 중 생성본 | 시작 시 한 번 생성한 prefab 인스턴스입니다. |

Reference Image Library에는 `Marker_01`~`Marker_06` 이미지 6장을 넣습니다. 각 이미지의 실제 출력 크기를 정확히 입력해야 위치와 스케일이 안정적입니다.

## 5. ARScene 구성

`ARScene`에서 사용자가 직접 다음 GameObject를 구성합니다.

```text
AR Session
XR Origin / AR Session Origin
 └─ AR Camera
BuildingContentRoot
 ├─ VirtualMarkers
 │   ├─ Marker_01
 │   ├─ ...
 │   └─ Marker_06
 └─ RouteContentRoot
MarkerRouteController
```

- `XR Origin / AR Session Origin`에는 `AR Tracked Image Manager`를 추가하고 Reference Image Library를 연결합니다.
- `BuildingContentRoot`는 처음에는 위치 `(0, 0, 0)`, 회전 `(0, 0, 0)`, 스케일 `(1, 1, 1)`로 둡니다.
- `VirtualMarkers`의 자식 `Marker_01`~`Marker_06`은 실제 건물에서 측정한 위치와 방향으로 배치합니다.
- 경로 prefab 인스턴스는 실행 중 `RouteContentRoot` 아래에 생성합니다. prefab 자체도 건물 좌표계 기준 위치로 제작합니다.
- `MarkerRouteController`에는 마커 데이터 6개와 `BuildingContentRoot`, `RouteContentRoot`, `AR Tracked Image Manager`를 할당합니다.

새 Unity C# 스크립트는 `NodeNavi_Unity/Assets/Data/Scripts`에 만들고 파일명에 `sh_` 접두사를 사용합니다.

## 6. 실행 시 초기화

1. `MarkerRouteController`가 마커 데이터 6개를 검사합니다. 이름 중복, 누락된 Transform, 누락된 prefab은 Console 오류로 알려 주고 시작하지 않습니다.
2. 각 `routePrefab`을 `RouteContentRoot` 아래에 한 번만 생성합니다.
3. 생성한 인스턴스는 해당 데이터의 `runtimeInstance`에 저장하고 모두 `SetActive(false)`로 비활성화합니다.
4. `ARTrackedImageManager.trackedImagesChanged` 이벤트를 구독합니다.
5. 오브젝트가 비활성화될 때 이벤트 구독을 해제합니다.

매번 prefab을 새로 생성하지 않으므로 같은 선로가 겹치거나 메모리가 계속 늘어나는 문제를 방지합니다.

## 7. 마커 기반 좌표계 정렬

추적 상태가 `Tracking`인 마커가 들어오면, 그 이름에 해당하는 마커 데이터를 찾습니다. 이후 다음 순서로 `BuildingContentRoot`를 정렬합니다.

1. `knownMarkerTransform`의 **로컬** 위치와 로컬 회전을 읽습니다. 이는 건물 좌표계에서의 알려진 마커 Pose입니다.
2. AR Foundation이 제공하는 실제 추적 이미지의 월드 위치와 회전을 읽습니다.
3. 가상 마커가 실제 마커와 일치하도록 `BuildingContentRoot`의 목표 위치·회전을 계산합니다.
4. 첫 번째 안정 인식에서는 즉시 목표 Pose를 적용합니다.
5. 이후 안정 인식에서는 짧게 보간하여 적용해 화면이 떨리거나 크게 튀는 느낌을 줄입니다.

개념식은 다음과 같습니다.

```text
rootRotation = trackedMarkerRotation × inverse(knownMarkerLocalRotation)
rootPosition = trackedMarkerPosition - rootRotation × knownMarkerLocalPosition
```

이 계산 후에는 `BuildingContentRoot` 아래의 모든 가상 마커와 선로 prefab이 함께 움직입니다. 따라서 마커 1에서 보던 선로와 마커 2에서 보이는 선로가 같은 건물 기준 위치를 공유합니다.

### 안정적인 보정 규칙

- `Tracking` 상태에서만 새 보정을 계산합니다.
- 같은 프레임에 여러 마커가 보여도 하나만 기준으로 선택합니다. 처음에는 현재 표시 중인 마커를 우선하고, 없으면 화면에 먼저 안정적으로 들어온 마커를 사용합니다.
- 마커가 새로 보인 직후 약 0.2~0.5초 동안 Pose가 안정적으로 유지된 뒤 보정합니다.
- 이미 정렬된 뒤에는 위치·회전 변화가 매우 작으면 갱신하지 않아 미세한 흔들림을 줄입니다.
- 오차가 큰 경우에는 보간 시간보다 즉시 정렬을 우선하고, UI에 `위치를 재정렬하고 있습니다`를 잠시 표시합니다.
- 마커 추적이 잠시 `Limited` 또는 `None`이 되어도 마지막 정렬 결과를 유지합니다. 콘텐츠를 원점으로 돌리거나 즉시 끄지 않습니다.

## 8. 최대 3개 경로 표시 정책

표시 기준은 최근 인식 순서의 큐가 아니라 **현재 경로 순서와 그 이전 두 구간**입니다. `routeOrder`가 0부터 시작한다고 가정합니다.

| 현재 경로 순서 | 활성화할 경로 |
| ---: | --- |
| 0 | 0 |
| 1 | 1, 0 |
| 2 | 2, 1, 0 |
| 4 | 4, 3, 2 |
| 5 | 5, 4, 3 |

처리 순서는 다음과 같습니다.

1. 안정적으로 인식한 마커의 `routeOrder`를 `currentRouteOrder`로 저장합니다.
2. 활성 범위를 `max(0, currentRouteOrder - 2)`부터 `currentRouteOrder`까지로 계산합니다.
3. 6개 `runtimeInstance`를 순회해 범위에 포함되는 것만 활성화합니다.
4. 범위 밖의 인스턴스는 비활성화합니다.

예를 들어 3번 구간에서 2번 구간으로 돌아오면, 3번은 꺼지고 2번과 1번만 남습니다. 실제 선로가 분기되는 경우에는 이후 `previousRouteIds`를 데이터에 추가해 “이전 두 경로” 규칙을 그래프 구조로 확장할 수 있습니다.

## 9. Inspector 설정 순서

1. Reference Image Library에 마커 이미지 6개를 추가하고 이름을 `Marker_01`~`Marker_06`으로 맞춥니다.
2. 각 이미지의 실제 인쇄 폭을 입력합니다.
3. `AR Tracked Image Manager`에 Reference Image Library를 할당합니다.
4. `BuildingContentRoot` 아래에 `VirtualMarkers`를 만들고, `Marker_01`~`Marker_06` 빈 오브젝트를 배치합니다.
5. 실제 도면과 줄자 측정값을 바탕으로 가상 마커의 위치·회전을 입력합니다.
6. 각 경로 prefab을 실제 선로의 건물 좌표 기준으로 제작합니다. 개별 마커 기준 원점으로 만들지 않습니다.
7. `MarkerRouteController`의 각 데이터 항목에 마커 이름, 경로 순서, 가상 마커 Transform, prefab을 연결합니다.
8. `BuildingContentRoot`, `RouteContentRoot`, `AR Tracked Image Manager` 참조가 모두 할당됐는지 확인합니다.

## 10. 테스트 절차

1. Unity Console에서 마커 데이터의 누락·중복 오류가 없는지 확인합니다.
2. 실제 Android 기기에서 1번 마커를 인식합니다. 선로가 현실 위치에 처음 정렬되는지 확인합니다.
3. 1→2→3번 순서로 이동해 표시 수가 1개→2개→3개가 되는지 확인합니다.
4. 5번 마커를 인식해 5·4·3번 구간만 보이는지 확인합니다.
5. 3번에서 2번으로 돌아가 3번 구간이 꺼지고 2·1번만 남는지 확인합니다.
6. 멀리 이동한 뒤 이미 인식한 다른 마커를 다시 비춰, 모든 활성 경로가 함께 재정렬되는지 확인합니다.
7. 같은 마커를 반복 인식해도 prefab 인스턴스가 추가로 생성되지 않는지 확인합니다.
8. 마커를 가렸다가 다시 비췄을 때 콘텐츠가 사라지거나 원점으로 이동하지 않고, 재인식 시 위치가 보정되는지 확인합니다.

## 11. 현장 배치 주의사항

- 마커는 평평한 표면에 단단히 붙이고, 강한 반사광·주름·반복 무늬를 피합니다.
- 마커마다 이미지가 충분히 달라야 오인식을 줄일 수 있습니다.
- 가상 마커 위치는 실제 건물 기준으로 정확히 측정합니다. 이 값의 오차는 선로 표시 오차로 이어집니다.
- 실제 기기 검증이 필수입니다. Unity Editor에서는 Inspector 연결과 활성화 규칙을 확인할 수 있지만, 실제 이미지 추적 품질과 현장 조명은 Android 기기에서만 확인할 수 있습니다.
