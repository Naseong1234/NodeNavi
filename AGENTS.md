# NodeNavi 작업 가이드

## 프로젝트 개요

NodeNavi는 건물 시설 관리와 문제 해결을 돕는 Android 기반 Unity AR Foundation 프로젝트다. 사용자는 `LoginScene`에서 버튼을 통해 `ARScene` 또는 `ExplaneScene`으로 이동한다.

- `ExplaneScene`은 앱 사용 방법을 텍스트로 안내한다.
- `ARScene`은 AR 마커 이미지를 인식해, 해당 위치에 연결된 시설 정보와 길 안내용 오브젝트를 표시한다.
- 기준 화면 해상도는 가로 1920 x 세로 1080이다. UI를 만들거나 수정할 때 이 기준을 유지한다.

대상 사용자는 프로젝트 구조와 Unity 경험이 많지 않을 수 있다. 설명과 작업 결과는 차분하고 단계적으로 작성하며, Unity Editor에서 사용자가 직접 수행해야 하는 일은 빠뜨리지 말고 안내한다.

## 폴더 및 에셋 규칙

- Unity 프로젝트의 루트는 `NodeNavi_Unity`이다. Unity 관련 작업은 이 폴더 안에서 수행한다.
- 새 Unity C# 스크립트는 반드시 `NodeNavi_Unity/Assets/Data/Scripts` 아래에 만든다.
- Unity에서 사용하는 자료가 아닌 문서, 원본 파일, 참고 자료 등은 별도 경로가 지정되지 않았다면 저장소 루트, 즉 이 `AGENTS.md`가 있는 `NodeNavi` 프로젝트 폴더에 둔다. 별도의 `NodeNavi` 하위 폴더를 새로 만들지 않는다.
- Unity가 관리하는 `.meta` 파일은 에셋과 함께 유지한다. `Library`, `Temp`, `Logs`, `obj`, `.vs`는 수정하거나 버전 관리 대상으로 추가하지 않는다.
- 새로 만드는 Unity C# 스크립트 파일명은 `sh_` 접두사를 사용한다. 클래스 이름도 파일명과 일치시킨다.

## AR 구현 방향

AR 마커 이미지 인식에는 AR Foundation의 이미지 추적 기능을 사용한다. 건물 내부 길 안내는 다음 방식을 기본으로 한다.

1. 마커별로 표시할 길 안내 오브젝트를 사전에 prefab으로 제작한다.
2. 필요한 prefab 인스턴스는 시작 시 생성한 뒤 비활성화한다. 또는 마커별 인스턴스를 필요할 때 생성한다.
3. 추적 이미지가 추가되거나 갱신될 때, 인식된 마커에 해당하는 오브젝트만 활성화하고 마커 위치와 자세에 맞춘다.
4. 추적이 중단되거나 다른 마커가 인식되면 이전 오브젝트는 비활성화하거나 제거해 화면에 남지 않게 한다.

우선은 위의 **사전 제작 prefab을 마커 인식으로 활성화/비활성화하는 방식**을 권장한다. 에셋 연결과 상태 확인이 쉬워 초보자가 Unity Inspector에서 점검하기 좋고, 이후 경로 데이터 기반 생성 방식으로 확장할 수도 있다.

## Unity Editor에서 사용자가 할 일

에이전트는 Scene 안의 GameObject 배치와 Transform 위치 조정을 대신 확정하지 않는다. 해당 작업이 필요한 경우 다음을 구체적으로 안내한다.

- 어떤 Scene을 열어야 하는지
- 어떤 GameObject에 어떤 컴포넌트를 추가하거나 연결해야 하는지
- Inspector에서 prefab, 참조, 이미지 라이브러리, 이벤트 필드에 무엇을 할당해야 하는지
- 사용자가 조정해야 할 위치, 회전, 크기와 확인 방법
- Android 기기에서 AR 기능을 검증하는 방법

코드에서 Inspector 할당이 필요한 public 또는 `[SerializeField]` 필드를 추가했다면, 필드의 목적과 정확한 할당 대상을 작업 결과에 함께 적는다.

## 작업 방식

- 요청받은 기능에 필요한 스크립트를 작성하고, 컴파일 오류나 프로젝트 오류가 보고되면 원인을 확인해 해결한다.
- 현재 프로젝트 구조와 기존 코드를 먼저 확인하고, 불필요하게 기존 에셋이나 설정을 덮어쓰지 않는다.
- 구현 후에는 변경 파일, 사용자가 Unity Editor에서 설정할 항목, 테스트 절차를 간결하게 정리한다.
- 이후 개선 요청이나 추가 요구사항이 들어오면 기존 프로젝트 맥락을 유지하면서 반영한다.

## C# 코드 스타일

- Unity의 일반적인 C# 관례를 따른다: 명확한 클래스/메서드 이름, PascalCase 타입과 public 멤버, camelCase 지역 변수와 private 필드를 사용한다.
- Inspector에서 설정할 필드는 가능한 한 `[SerializeField] private`로 선언하고, null 참조와 잘못된 설정에 대한 방어 코드를 둔다.
- 매 프레임 처리와 이벤트 처리의 책임을 분리하고, 이미지 추적 이벤트는 활성화 시 구독하고 비활성화 시 해제한다.
- AR 세션, 이미지 라이브러리, prefab 연결처럼 Android 기기에서만 검증 가능한 항목은 Editor 플레이 모드와 실제 기기 검증의 차이를 명시한다.
